git clone https://github.com/alkhamov/vm-micro2.git

terraform init

terraform plan \
  -var="project_id=$(gcloud config get-value project)"

terraform apply \
  -var="project_id=$(gcloud config get-value project)"
